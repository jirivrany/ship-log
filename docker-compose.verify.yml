services:
  verify:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - .:/app
      - ./data-verify:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/ship_log.db
      - UPLOAD_DIR=/app/data/uploads
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
